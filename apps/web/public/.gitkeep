Next.js serves static files from this directory at the site root.

It is intentionally empty: every illustration in the product is inline SVG generated by the
design system, so there are no bitmap or font assets to ship. The directory is kept because
the Docker image copies it, and because anything added here (robots.txt, a social preview
image) is served with no further configuration.
